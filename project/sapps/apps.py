# from django.apps import AppConfig

# class AppConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'sapps'

#     def ready(self):
#         import sapps.signals  # ensures signals are registered
